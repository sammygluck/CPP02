/*

main()
{
    numbers = parsing;
    vector_array = to_vector(numbers);
    list_array = to_list(numbers);

    sort_vector(vector_array);
}

sort_vector(vector_array)
{
    new_vector;

    for each number in vector_array
        check if vector_array[i] > vector_array[i + 1]
            array[2] = [vector_array[larger], vector_array[smaller];
            add_array_to_new_vector;
            increment i++ i++;

    new_vector2 = sort_new_vector_recursively;
    new_vector = main_chain(new_vector2);
    pend_chain = pend_chain(new_vector2);

    insert_using_jacobstal_into_new_vector;
}

insert()
{
    pend_a = pend.begin();
    pend_b = pend.begin();
}

*/